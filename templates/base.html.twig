<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>{% block title %}Welcome!{% endblock %}</title>
    {% block stylesheets %}{% endblock %}
    <style>
        * {
            margin: 0 auto;
        }

        body {
            text-align: center;
            background-color: #6E6E6E;
        }

        header {
            width: 100%;
            height: 70px;
            background-color: #6E6E6E;
        }

        header h1 {
            line-height: 35px;
        }

        .wrapper {
            width: 100%;
            height: 500px;
            background-color: #eeeeee;
            padding-top: 50px;
        }

        .message {
            width: 100%;
            height: auto;
            background-color: #eeeeee;
            padding-top: 20px;
            text-align: center;
        }

        .message .message-wrapper {
            width: 70%;
            height: auto;
            background-color: #eeeeee;
            text-align: left;
        }

        .message .message-wrapper .flash-notice-success {
            width: 100%;
            height: auto;
            background-color: green;
            text-align: left;
        }

        .message .message-wrapper .flash-notice-error {
            width: 100%;
            height: auto;
            background-color: indianred;
            text-align: left;
        }

        .form_fields {
            width: 80%;
        }

        .form_fields .label {
            width: 100%;
            color: black;
            text-align: left;
            margin-top: 10px;
        }

        .form_fields .field {
            width: 100%;
        }

        .form_fields .field_checkbox {
            text-align: left;
            width: 100%;
            margin-top: 10px;
        }

        .form_fields .field input {
            width: 100%;
            height: 25px;
            border: #313131 1px;
            border-radius: 5px;
        }

        .form_fields .field select {
            width: 100%;
            height: 25px;
            border: #313131 1px;
            border-radius: 5px;
        }

        .form_fields .save_butt {
            width: 100%;
        }

        .form_fields .save_butt button {
            width: 80px;
            height: 30px;
            color: #fff;
            background-color: #313131;
            margin-top: 20px;
            border: 0px;
            border-radius: 5px;
        }

        button a {
            color: #ffffff;
            text-decoration: none;
        }


        .form_fields .form_title {
            width: 100%;
            text-align: left;
        }

        .helper-button {
            width: 90%;
            height: auto;
            text-align: left;
            margin-bottom: 20px;
        }

        .helper-button button {
            width: 100px;
            height: 40px;
            color: #fff;
            background-color: #313131;
            margin-top: 20px;
            border: 0px;
            border-radius: 5px;
        }

        footer {
            width: 100%;
            height: 30px;
            background-color: #6E6E6E;
        }
    </style>
</head>
<body>
<header>
    <h1>Digikala Task</h1>
</header>
<div class="message">
    <div class="message-wrapper">
        <div class="flash-notice-success">
            {% for message in app.flashes('success') %}
                <div class="flash-notice">
                    <span><h3>Success: </h3></span>
                    <span><p>{{ message }}</p></span>
                </div>
            {% endfor %}

        </div>
        {% for message in app.flashes('success') %}
            <div class="flash-notice-error">
                <span><h3>Error: </h3></span>
                <span><p>{{ message }}</p></span>
            </div>
        {% endfor %}
    </div>
</div>


{% block body %}{% endblock %}
{% block javascripts %}{% endblock %}
<footer>

</footer>
</body>
</html>
