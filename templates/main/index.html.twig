{% extends 'base.html.twig' %}

{% block title %}Hello MainController!{% endblock %}

{% block body %}
    <style>
        .manage_butt {
            width: 70%;
            height: auto;
            line-height: 300px;
        }

        .manage_butt button {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            background-color: #313131;
            color: #ffffff;
            margin-left: 50px;
            border: 0px;
        }

        .manage_butt a {
            text-decoration: none;
        }

        .manage_butt a:hover {
            cursor: pointer;
        }
    </style>

    <div class="wrapper">
        <div class="manage_butt">
        <span>
            <a href="{{ app.request.getBaseURL() }}/product/list">
            <button class="btn btn-lg btn-primary" type="button">
                Manage Products
            </button>
                </a>
        </span>
            <span>
            <button class="btn btn-lg btn-primary" type="button">
                <a href="{{ app.request.getBaseURL() }}/variant/list">Manage Variants </a>
            </button>
        </span>
        </div>
    </div>
{% endblock %}
