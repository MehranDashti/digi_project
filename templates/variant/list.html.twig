{% extends 'base.html.twig' %}

{% block title %}Variants List{% endblock %}

{% block body %}
    <style>
        .wrapper {
            width: 100%;
            min-height: 500px;
            height: auto;
            background-color: #eeeeee;
            padding-top: 50px;
            margin-bottom: 50px;
        }

        table {
            width: 90%;
            height: auto;
            border: #313131 4px double;
        }

        tr {
            width: 100%;
        }

        td {
            width: 25%;
        }

        tr * {
            border: #313131 1px solid;
        }
    </style>


    <div class="wrapper">
        <div class="helper-button">
            <button class="btn btn-lg btn-primary" type="button">
                <a href="{{ app.request.getBaseURL() }}/variant/add">Add Variants </a>
            </button>
        </div>
        <table>
            <tr>
                <th> Name</th>
                <th> Description</th>
                <th> Product</th>
                <th> Action</th>
            </tr>
            {% for item in variants %}
                <tr>
                    <td>{{ item.getColor }}</td>
                    <td>{{ item.getPrice }}</td>
                    <td>{{ item.getProduct.getTitle }}</td>
                    <td>
                        <a href="{{ app.request.getBaseURL() }}/variant/edit/{{ item.getId }}">Edit</a>
                        <a href="{{ app.request.getBaseURL() }}/variant/delete/{{ item.getId }}">delete</a>
                    </td>
                </tr>
            {% endfor %}
        </table>
    </div>
{% endblock %}

